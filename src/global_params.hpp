#ifndef GLOBAL_PARAM
#define GLOBAL_PARAM

namespace global_params {
    int ctrl_size; 
    int bd_size; 
    float best_fit; 
    int best_fit_index; 
    std::vector<float> best_fit_controller; 
}

#endif